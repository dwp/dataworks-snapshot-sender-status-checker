groups:
  - name: master
    jobs:
      - management-dev
      - management
  - name: pull-request
    jobs:
      - dataworks-snapshot-sender-status-checker-pr
  - name: mirror
    jobs:
      - mirror-dwpdigital-dataworks-snapshot-sender-status-checker-dev
      - mirror-dwpdigital-dataworks-snapshot-sender-status-checker
