resources:
  - name: dataworks-snapshot-sender-status-checker-pr
    type: pull-request
    source:
      repository: dwp/dataworks-snapshot-sender-status-checker
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h

  - name: dataworks-snapshot-sender-status-checker
    type: git
    source:
      branch: master
      uri: https://github.com/dwp/dataworks-snapshot-sender-status-checker.git
      access_token: ((dataworks-secrets.concourse_github_pat))
    webhook_token: ((dataworks.concourse_github_webhook_token))
    check_every: 720h
